{
  "dependencies": {
    "apollo": "2.33.9",
    "husky": "7.0.4"
  },
  "scripts": {
    "graphql": "apollo codegen:generate --localSchemaFile=schema.graphql --includes='graphql/*.graphql' --target=swift LastDram/API.generated.swift",
    "lint": "swiftlint lint --strict --reporter github-actions-logging",
    "lint:fix": "swiftlint lint --fix",
    "prepare": "husky install"
  }
}
