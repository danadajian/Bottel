{
  "dependencies": {
    "apollo": "2.33.9"
  },
  "scripts": {
    "graphql": "apollo codegen:generate --localSchemaFile=schema.graphql --includes='graphql/*.graphql' --target=swift Bottel/API.swift"
  }
}
