opt_out_usage

default_platform(:ios)

platform :ios do
  desc "Run unit tests"
  lane :test do
    scan(scheme: "LastDramTests")
  end

  desc "build"
  lane :build do
    sh("./import-certificate.sh")
    sh("./import-profile.sh")
    sh("./set-env-vars.sh")
    build_app
  end
end
